name: Build & Publish Docker Images

on:
  push:
    branches:
      - beta

jobs:
  #

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  api-server:
    name: 'Docker Build - server'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Login to GitHub Container Registry
        uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        uses: docker/build-push-action@master
        with:
          push: true
          context: ./server
          file: ./server/Dockerfile
          tags: ghcr.io/carrismetropolitana/api-server:${{ github.ref_name }}

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  api-parse-datasets:
    name: 'Docker Build - parse-datasets'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Login to GitHub Container Registry
        uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        uses: docker/build-push-action@master
        with:
          push: true
          context: ./parse-datasets
          file: ./parse-datasets/Dockerfile
          tags: ghcr.io/carrismetropolitana/api-parse-datasets:${{ github.ref_name }}

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  api-parse-network:
    name: 'Docker Build - parse-network'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Login to GitHub Container Registry
        uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        uses: docker/build-push-action@master
        with:
          push: true
          context: ./parse-network
          file: ./parse-network/Dockerfile
          tags: ghcr.io/carrismetropolitana/api-parse-network:${{ github.ref_name }}

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  api-sync-rtevents:
    name: 'Docker Build - sync-rtevents'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Calculate current hash for sync-rtevents
        id: calculate-hash
        run: |
          find sync-rtevents -type f -exec sha256sum {} + | sort | sha256sum > sync-rtevents.hash

      - name: Cache previous hash
        id: cache-previous-hash
        uses: actions/cache@v3
        with:
          path: sync-rtevents.hash.old
          key: sync-rtevents-hash-${{ github.sha }}
          restore-keys: sync-rtevents-hash-

      - name: Compare hashes
        id: compare-hash
        run: |
          if cmp -s sync-rtevents.hash sync-rtevents.hash.old; then
            echo "sync-rtevents unchanged"
            echo "sync_rtevents_changed=false" >> $GITHUB_ENV
          else
            echo "sync-rtevents changed"
            echo "sync_rtevents_chang

  # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  api-sync-encm:
    name: 'Docker Build - sync-encm'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Login to GitHub Container Registry
        uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        uses: docker/build-push-action@master
        with:
          push: true
          context: ./sync-encm
          file: ./sync-encm/Dockerfile
          tags: ghcr.io/carrismetropolitana/api-sync-encm:${{ github.ref_name }}

          # # # # # # # # # # # # # # # # # # # # #
  # # # # # # # # # # # # # # # # # # # # #

  api-switch:
    name: 'Docker Build - switch'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Login to GitHub Container Registry
        uses: docker/login-action@master
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker Image
        uses: docker/build-push-action@master
        with:
          push: true
          context: ./switch
          file: ./switch/Dockerfile
          tags: ghcr.io/carrismetropolitana/api-switch:${{ github.ref_name }}
