{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": ["./secrets/*"],
	"globalEnv": ["*"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"dev": {
			"cache": false,
			"passThroughEnv": ["*"],
			"persistent": true
		},
		"dev:sync": {
			"cache": false,
			"persistent": true
		},
		"start": {
			"dependsOn": ["^build", "^start"],
			"outputs": ["dist/**"]
		},
		"start:sync": {
			"dependsOn": ["^start:sync"],
			"outputs": ["dist/**"]
		}
	},
	"ui": "tui"
}
