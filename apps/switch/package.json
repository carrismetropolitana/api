{
	"name": "@carrismetropolitana/api-switch",
	"version": "1.0.0",
	"type": "module",
	"files": ["dist"],
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "npm run clean && tsc && resolve-tspaths",
		"clean": "rm -rf dist",
		"dev:none": "tsx watch --env-file=.env.local ./src/index.ts",
		"lint": "eslint .",
		"lint:fix": "eslint . --fix",
		"start:none": "npm run build && node dist/index.js"
	},
	"dependencies": {
		"fastify": "5.0.0"
	},
	"devDependencies": {
		"@types/node": "22.7.5",
		"resolve-tspaths": "0.8.22",
		"typescript": "5.6.3"
	}
}
